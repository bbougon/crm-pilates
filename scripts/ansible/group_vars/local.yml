root_dir: "{{ (playbook_dir + '/../..') | realpath }}"
event_store_path: "{{ root_dir }}/local.persistent/event/event_store.db"
cors_allow_origins: "*"
database_url: "postgresql://crm-pilates-test:example@localhost:5433/crm-pilates-test"